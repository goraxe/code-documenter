zenuml
    path.extension()?.to_str()
    path.extension()
    Some(Language::Rust)
    Some(Language::Go)
    Some(Language::TypeScript)
    Box.new(rust::RustParser)
    Box.new(go::GoParser)
    Box.new(typescript::TypeScriptParser)
    root.walk()
    root.children(&mut cursor)
    child.kind()
    parse_struct(&child, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        node.child_by_field_name("body")
        field_list.walk()
        field_list.children(&mut cursor)
        child.kind()
        parse_field_declaration(&child, source)
        fields.push(field)
        Some(Entity {
        name,
        kind: EntityKind::Struct,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        file.to_string()
    }
    infer_field_relationships(&entity, &mut model.relationships) {
        infer_from_type(owner, &field.name, &field.type_info, relationships)
    }
    model.entities.push(entity)
    parse_enum(&child, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        node.child_by_field_name("body")
        variant_list.walk()
        variant_list.children(&mut cursor)
        child.kind()
        child_by_field(&child, "name", source)
        fields.push(Field {
                        name: variant_name,
                        type_info: TypeInfo::Simple("()".to_string()),
                        visibility: Visibility::Public,
                    })
        TypeInfo.Simple("()".to_string())
        "()".to_string()
        Some(Entity {
        name,
        kind: EntityKind::Enum,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    parse_trait(&child, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        node.child_by_field_name("body")
        body.walk()
        body.children(&mut cursor)
        child.kind()
        parse_method_signature(&child, source, true)
        methods.push(method)
        parse_method_from_function(&child, source)
        methods.push(method)
        Some(Entity {
        name,
        kind: EntityKind::Trait,
        fields: Vec::new(),
        methods,
        source_file: file.to_string(),
    })
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    parse_function(&child, source, file) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        Vec.new()
        node.child_by_field_name("body")
        collect_call_exprs(&body, source, &mut calls)
        Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
        file.to_string()
    }
    model.functions.push(func)
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    field_list.walk()
    field_list.children(&mut cursor)
    child.kind()
    parse_field_declaration(&child, source) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        node.child_by_field_name("type")
        parse_type_node(&type_node, source)
        Some(Field {
        name,
        type_info,
        visibility,
    })
    }
    fields.push(field)
    Some(Entity {
        name,
        kind: EntityKind::Struct,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
    Vec.new()
    file.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_visibility(node, source) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        node_text(&child, source)
        text.starts_with("pub")
    }
    node.child_by_field_name("type")
    parse_type_node(&type_node, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    Some(Field {
        name,
        type_info,
        visibility,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    variant_list.walk()
    variant_list.children(&mut cursor)
    child.kind()
    child_by_field(&child, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    fields.push(Field {
                        name: variant_name,
                        type_info: TypeInfo::Simple("()".to_string()),
                        visibility: Visibility::Public,
                    })
    TypeInfo.Simple("()".to_string())
    "()".to_string()
    Some(Entity {
        name,
        kind: EntityKind::Enum,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
    Vec.new()
    file.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    body.walk()
    body.children(&mut cursor)
    child.kind()
    parse_method_signature(&child, source, true) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        has_self_parameter(node, source)
        Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract,
    })
    }
    methods.push(method)
    parse_method_from_function(&child, source) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        has_self_parameter(node, source)
        Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract: false,
    })
    }
    methods.push(method)
    Some(Entity {
        name,
        kind: EntityKind::Trait,
        fields: Vec::new(),
        methods,
        source_file: file.to_string(),
    })
    Vec.new()
    file.to_string()
    root.walk()
    root.children(&mut cursor)
    child.kind()
    process_impl_item(&child, source, file, model) {
        node.child_by_field_name("type")
        node_text(&type_node, source)
        node
        .child_by_field_name("trait").map(|n| node_text(&n, source))
        node.child_by_field_name("trait")
        node_text(&n, source)
        model.relationships.push(Relationship::Implementation {
            implementor: type_name.clone(),
            interface: trait_n.clone(),
        })
        type_name.clone()
        trait_n.clone()
        node.child_by_field_name("body")
        body.walk()
        body.children(&mut body_cursor)
        child.kind()
        parse_method_from_function(&child, source)
        attach_method_to_entity(model, &type_name, method, file)
    }
    node.child_by_field_name("type")
    node_text(&type_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node
        .child_by_field_name("trait").map(|n| node_text(&n, source))
    node.child_by_field_name("trait")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    model.relationships.push(Relationship::Implementation {
            implementor: type_name.clone(),
            interface: trait_n.clone(),
        })
    type_name.clone()
    trait_n.clone()
    node.child_by_field_name("body")
    body.walk()
    body.children(&mut body_cursor)
    child.kind()
    parse_method_from_function(&child, source) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        has_self_parameter(node, source)
        Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract: false,
    })
    }
    attach_method_to_entity(model, &type_name, method, file) {
        model.entities.iter_mut().find(|e| e.name == type_name)
        model.entities.iter_mut()
        entity.methods.push(method)
        type_name.to_string()
        Vec.new()
        file.to_string()
        model.entities.push(entity)
    }
    model.entities.iter_mut().find(|e| e.name == type_name)
    model.entities.iter_mut()
    entity.methods.push(method)
    type_name.to_string()
    Vec.new()
    file.to_string()
    model.entities.push(entity)
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_visibility(node, source) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        node_text(&child, source)
        text.starts_with("pub")
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_return_type(node, source) {
        node.child_by_field_name("return_type")
        Some(parse_type_node(&ret, source))
        parse_type_node(&ret, source)
    }
    has_self_parameter(node, source) {
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        node_text(&child, source)
    }
    Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_visibility(node, source) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        node_text(&child, source)
        text.starts_with("pub")
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_return_type(node, source) {
        node.child_by_field_name("return_type")
        Some(parse_type_node(&ret, source))
        parse_type_node(&ret, source)
    }
    has_self_parameter(node, source) {
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        node_text(&child, source)
    }
    Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract: false,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_visibility(node, source) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        node_text(&child, source)
        text.starts_with("pub")
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_return_type(node, source) {
        node.child_by_field_name("return_type")
        Some(parse_type_node(&ret, source))
        parse_type_node(&ret, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    collect_call_exprs(&body, source, &mut calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
    file.to_string()
    Vec.new()
    node.child_by_field_name("parameters")
    param_list.walk()
    param_list.children(&mut cursor)
    child.kind()
    parse_single_parameter(&child, source) {
        child_by_field(node, "pattern", source)
        node.child_by_field_name("type")
        parse_type_node(&type_node, source)
        Some(Parameter { name, type_info })
    }
    params.push(param)
    child_by_field(node, "pattern", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    node.child_by_field_name("type")
    parse_type_node(&type_node, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    Some(Parameter { name, type_info })
    node.child_by_field_name("return_type")
    Some(parse_type_node(&ret, source))
    parse_type_node(&ret, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    node.child_by_field_name("parameters")
    param_list.walk()
    param_list.children(&mut cursor)
    child.kind()
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.kind()
    parse_call_expr(node, source) {
        node.child_by_field_name("function")
        extract_arguments(node, source)
        func_node.kind()
        func_node
                .child_by_field_name("value").map(|n| node_text(&n, source))
        func_node.child_by_field_name("value")
        node_text(&n, source)
        func_node
                .child_by_field_name("field")
                .map(|n| node_text(&n, source)).unwrap_or_default()
        func_node
                .child_by_field_name("field").map(|n| node_text(&n, source))
        func_node.child_by_field_name("field")
        node_text(&n, source)
        Some(CallExpr {
                receiver,
                method,
                arguments,
            })
        node_text(&func_node, source)
        text.rsplit_once("::")
        Some(CallExpr {
                    receiver: Some(recv.to_string()),
                    method: method.to_string(),
                    arguments,
                })
        Some(recv.to_string())
        recv.to_string()
        method.to_string()
        Some(CallExpr {
                    receiver: None,
                    method: text,
                    arguments,
                })
        node_text(&func_node, source)
        Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    }
    calls.push(call)
    node.walk()
    node.children(&mut cursor)
    collect_call_exprs(&child, source, calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    node.child_by_field_name("function")
    extract_arguments(node, source) {
        Vec.new()
        node.child_by_field_name("arguments")
        arg_list.walk()
        arg_list.children(&mut cursor)
        child.kind()
        child.kind()
        child.kind()
        args.push(node_text(&child, source))
        node_text(&child, source)
    }
    func_node.kind()
    func_node
                .child_by_field_name("value").map(|n| node_text(&n, source))
    func_node.child_by_field_name("value")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    func_node
                .child_by_field_name("field")
                .map(|n| node_text(&n, source)).unwrap_or_default()
    func_node
                .child_by_field_name("field").map(|n| node_text(&n, source))
    func_node.child_by_field_name("field")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver,
                method,
                arguments,
            })
    node_text(&func_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    text.rsplit_once("::")
    Some(CallExpr {
                    receiver: Some(recv.to_string()),
                    method: method.to_string(),
                    arguments,
                })
    Some(recv.to_string())
    recv.to_string()
    method.to_string()
    Some(CallExpr {
                    receiver: None,
                    method: text,
                    arguments,
                })
    node_text(&func_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    Vec.new()
    node.child_by_field_name("arguments")
    arg_list.walk()
    arg_list.children(&mut cursor)
    child.kind()
    child.kind()
    child.kind()
    args.push(node_text(&child, source))
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.kind()
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    parse_generic_type(node, source) {
        node
        .child_by_field_name("type")
        .map(|n| node_text(&n, source)).unwrap_or_default()
        node
        .child_by_field_name("type").map(|n| node_text(&n, source))
        node.child_by_field_name("type")
        node_text(&n, source)
        Vec.new()
        node.child_by_field_name("type_arguments")
        type_args.walk()
        type_args.children(&mut cursor)
        child.is_named()
        params.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        base.as_str()
        params.len()
        TypeInfo.Optional(Box::new(params.remove(0)))
        Box.new(params.remove(0))
        params.remove(0)
        params.len()
        TypeInfo.Collection(Box::new(params.remove(0)))
        Box.new(params.remove(0))
        params.remove(0)
    }
    node.child_by_field_name("type")
    TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
    Box.new(parse_type_node(&inner, source))
    parse_type_node(&inner, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Vec.new()
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    items.push(parse_type_node(&child, source))
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Tuple(items)
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node
        .child_by_field_name("type")
        .map(|n| node_text(&n, source)).unwrap_or_default()
    node
        .child_by_field_name("type").map(|n| node_text(&n, source))
    node.child_by_field_name("type")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Vec.new()
    node.child_by_field_name("type_arguments")
    type_args.walk()
    type_args.children(&mut cursor)
    child.is_named()
    params.push(parse_type_node(&child, source))
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    base.as_str()
    params.len()
    TypeInfo.Optional(Box::new(params.remove(0)))
    Box.new(params.remove(0))
    params.remove(0)
    params.len()
    TypeInfo.Collection(Box::new(params.remove(0)))
    Box.new(params.remove(0))
    params.remove(0)
    infer_from_type(owner, &field.name, &field.type_info, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
    owner.to_string()
    name.clone()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
    owner.to_string()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
    owner.to_string()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
    owner.to_string()
    field_name.to_string()
    base.as_str()
    params.first()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
    owner.to_string()
    field_name.to_string()
    params.first()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
    owner.to_string()
    field_name.to_string()
    infer_from_type(owner, field_name, param, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    infer_from_type(owner, field_name, item, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    name.is_empty()
    name.chars().next().unwrap()
    name.chars().next()
    name.chars()
    first.is_uppercase()
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    Some(name.clone())
    name.clone()
    source[node.byte_range()].to_string()
    node.byte_range()
    node.child_by_field_name(field).map(|n| node_text(&n, source))
    node.child_by_field_name(field)
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.walk()
    node.children(&mut cursor)
    child.kind()
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    text.starts_with("pub")
    root.walk()
    root.children(&mut cursor)
    child.kind()
    process_type_declaration(&child, source, file, model) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        process_type_spec(&child, source, file, model)
    }
    parse_function(&child, source, file) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        Vec.new()
        node.child_by_field_name("body")
        collect_call_exprs(&body, source, &mut calls)
        Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
        file.to_string()
    }
    model.functions.push(func)
    process_method_declaration(&child, source, file, model) {
        child_by_field(node, "name", source)
        extract_receiver_type(node, source)
        go_visibility(&name)
        parse_method_parameters(node, source)
        parse_result(node, source)
        Vec.new()
        node.child_by_field_name("body")
        collect_call_exprs(&body, source, &mut calls)
        name.clone()
        calls.is_empty()
        model.functions.push(Function {
            name: format!("{}.{}", receiver_type, name),
            parameters: Vec::new(),
            return_type: None,
            visibility: go_visibility(&name),
            calls,
            source_file: file.to_string(),
        })
        Vec.new()
        go_visibility(&name)
        file.to_string()
        attach_method_to_entity(model, &receiver_type, method, file)
    }
    node.walk()
    node.children(&mut cursor)
    child.kind()
    process_type_spec(&child, source, file, model) {
        child_by_field(node, "name", source)
        node.child_by_field_name("type")
        type_node.kind()
        parse_struct(&name, &type_node, source, file)
        infer_field_relationships(&entity, &mut model.relationships)
        model.entities.push(entity)
        parse_interface(&name, &type_node, source, file)
        model.entities.push(entity)
    }
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    node.child_by_field_name("type")
    type_node.kind()
    parse_struct(&name, &type_node, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        node.child_by_field_name("body")
        field_list.walk()
        field_list.children(&mut cursor)
        child.kind()
        parse_field_declaration(&child, source)
        fields.push(field)
        Some(Entity {
        name,
        kind: EntityKind::Struct,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        file.to_string()
    }
    infer_field_relationships(&entity, &mut model.relationships) {
        infer_from_type(owner, &field.name, &field.type_info, relationships)
    }
    model.entities.push(entity)
    parse_interface(&name, &type_node, source, file) {
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.kind()
        child.kind()
        parse_method_elem(&child, source)
        methods.push(method)
        Some(Entity {
        name: name.to_string(),
        kind: EntityKind::Interface,
        fields: Vec::new(),
        methods,
        source_file: file.to_string(),
    })
        name.to_string()
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    Vec.new()
    node.child_by_field_name("body")
    parse_field_list(&field_list, source, name, &mut fields) {
        field_list.walk()
        field_list.children(&mut cursor)
        child.kind()
        parse_field_declaration(&child, source, fields)
    }
    node.walk()
    node.children(&mut cursor)
    child.kind()
    parse_field_list(&child, source, name, &mut fields) {
        field_list.walk()
        field_list.children(&mut cursor)
        child.kind()
        parse_field_declaration(&child, source, fields)
    }
    Some(Entity {
        name: name.to_string(),
        kind: EntityKind::Struct,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
    name.to_string()
    Vec.new()
    file.to_string()
    field_list.walk()
    field_list.children(&mut cursor)
    child.kind()
    parse_field_declaration(&child, source, fields) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        node.child_by_field_name("type")
        parse_type_node(&type_node, source)
        Some(Field {
        name,
        type_info,
        visibility,
    })
    }
    Vec.new()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    names.push(node_text(&child, source))
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    is_type_node(&child) {
        is_type_node_kind(node.kind())
        node.kind()
    }
    Some(child)
    names.is_empty()
    extract_embedded_name(tn, source) {
        node.kind()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        extract_embedded_name(&child, source)
        node_text(node, source)
        node.child_by_field_name("name")
        node_text(&name_node, source)
        node_text(node, source)
        node_text(node, source)
    }
    parse_type_node(tn, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    go_visibility(&embedded_name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    fields.push(Field {
                name: embedded_name,
                type_info,
                visibility,
            })
    parse_type_node(tn, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    go_visibility(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    fields.push(Field {
                    name: name.clone(),
                    type_info: clone_type_info(&type_info),
                    visibility,
                })
    name.clone()
    clone_type_info(&type_info) {
        TypeInfo.Simple(s.clone())
        s.clone()
        base.clone()
        params.iter().map(clone_type_info).collect()
        params.iter().map(clone_type_info)
        params.iter()
        TypeInfo.Optional(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Collection(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Tuple(items.iter().map(clone_type_info).collect())
        items.iter().map(clone_type_info).collect()
        items.iter().map(clone_type_info)
        items.iter()
        TypeInfo.Reference(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
    }
    node.kind()
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    extract_embedded_name(&child, source) {
        node.kind()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        extract_embedded_name(&child, source)
        node_text(node, source)
        node.child_by_field_name("name")
        node_text(&name_node, source)
        node_text(node, source)
        node_text(node, source)
    }
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.child_by_field_name("name")
    node_text(&name_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    is_type_node_kind(node.kind())
    node.kind()
    Vec.new()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    child.kind()
    parse_method_elem(&child, source) {
        child_by_field(node, "name", source).or_else(|| {
        let mut cursor = node.walk();
        for child in node.children(&mut cursor) {
            if child.kind() == "field_identifier" {
                return Some(node_text(&child, source));
            }
        }
        None
    })
        child_by_field(node, "name", source)
        node.walk()
        node.children(&mut cursor)
        child.kind()
        Some(node_text(&child, source))
        node_text(&child, source)
        go_visibility(&name)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.kind()
        param_lists.push(child)
        is_type_node_kind(k)
        Some(child)
        Vec.new()
        param_lists.first()
        collect_parameters_from_list(first_params, source, &mut parameters)
        param_lists.len()
        Vec.new()
        ret_list.walk()
        ret_list.children(&mut ret_cursor)
        child.kind()
        child.child_by_field_name("type")
        items.push(parse_type_node(&type_n, source))
        parse_type_node(&type_n, source)
        items.len()
        Some(items.remove(0))
        items.remove(0)
        items.is_empty()
        Some(TypeInfo::Tuple(items))
        TypeInfo.Tuple(items)
        return_type_node.as_ref().map(|rtn| parse_type_node(rtn, source))
        return_type_node.as_ref()
        parse_type_node(rtn, source)
        Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static: false,
        is_abstract: true,
    })
    }
    methods.push(method)
    Some(Entity {
        name: name.to_string(),
        kind: EntityKind::Interface,
        fields: Vec::new(),
        methods,
        source_file: file.to_string(),
    })
    name.to_string()
    Vec.new()
    file.to_string()
    child_by_field(node, "name", source).or_else(|| {
        let mut cursor = node.walk();
        for child in node.children(&mut cursor) {
            if child.kind() == "field_identifier" {
                return Some(node_text(&child, source));
            }
        }
        None
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    node.walk()
    node.children(&mut cursor)
    child.kind()
    Some(node_text(&child, source))
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    go_visibility(&name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    Vec.new()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    param_lists.push(child)
    is_type_node_kind(k)
    Some(child)
    Vec.new()
    param_lists.first()
    collect_parameters_from_list(first_params, source, &mut parameters) {
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_parameter_declaration(&child, source, params)
    }
    param_lists.len()
    Vec.new()
    ret_list.walk()
    ret_list.children(&mut ret_cursor)
    child.kind()
    child.child_by_field_name("type")
    items.push(parse_type_node(&type_n, source))
    parse_type_node(&type_n, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    items.len()
    Some(items.remove(0))
    items.remove(0)
    items.is_empty()
    Some(TypeInfo::Tuple(items))
    TypeInfo.Tuple(items)
    return_type_node.as_ref().map(|rtn| parse_type_node(rtn, source))
    return_type_node.as_ref()
    parse_type_node(rtn, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static: false,
        is_abstract: true,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    go_visibility(&name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_result(node, source) {
        node.child_by_field_name("result")
        result_node.kind()
        Vec.new()
        result_node.walk()
        result_node.children(&mut cursor)
        child.kind()
        child.child_by_field_name("type")
        items.push(parse_type_node(&type_n, source))
        parse_type_node(&type_n, source)
        items.len()
        Some(items.remove(0))
        items.remove(0)
        items.is_empty()
        Some(TypeInfo::Tuple(items))
        TypeInfo.Tuple(items)
        Some(parse_type_node(&result_node, source))
        parse_type_node(&result_node, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    collect_call_exprs(&body, source, &mut calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
    file.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_receiver_type(node, source) {
        node.child_by_field_name("receiver")
        receiver.walk()
        receiver.children(&mut cursor)
        child.kind()
        child.child_by_field_name("type")
        Some(extract_base_type_name(&type_node, source))
        extract_base_type_name(&type_node, source)
    }
    go_visibility(&name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    parse_method_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        collect_parameters_from_list(&param_list, source, &mut params)
    }
    parse_result(node, source) {
        node.child_by_field_name("result")
        result_node.kind()
        Vec.new()
        result_node.walk()
        result_node.children(&mut cursor)
        child.kind()
        child.child_by_field_name("type")
        items.push(parse_type_node(&type_n, source))
        parse_type_node(&type_n, source)
        items.len()
        Some(items.remove(0))
        items.remove(0)
        items.is_empty()
        Some(TypeInfo::Tuple(items))
        TypeInfo.Tuple(items)
        Some(parse_type_node(&result_node, source))
        parse_type_node(&result_node, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    collect_call_exprs(&body, source, &mut calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    name.clone()
    calls.is_empty()
    model.functions.push(Function {
            name: format!("{}.{}", receiver_type, name),
            parameters: Vec::new(),
            return_type: None,
            visibility: go_visibility(&name),
            calls,
            source_file: file.to_string(),
        })
    Vec.new()
    go_visibility(&name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    file.to_string()
    attach_method_to_entity(model, &receiver_type, method, file) {
        model.entities.iter_mut().find(|e| e.name == type_name)
        model.entities.iter_mut()
        entity.methods.push(method)
        type_name.to_string()
        Vec.new()
        file.to_string()
        model.entities.push(entity)
    }
    node.child_by_field_name("receiver")
    receiver.walk()
    receiver.children(&mut cursor)
    child.kind()
    child.child_by_field_name("type")
    Some(extract_base_type_name(&type_node, source))
    extract_base_type_name(&type_node, source) {
        node.kind()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        extract_base_type_name(&child, source)
        node_text(node, source)
        node_text(node, source)
    }
    node.kind()
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    extract_base_type_name(&child, source) {
        node.kind()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        extract_base_type_name(&child, source)
        node_text(node, source)
        node_text(node, source)
    }
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    model.entities.iter_mut().find(|e| e.name == type_name)
    model.entities.iter_mut()
    entity.methods.push(method)
    type_name.to_string()
    Vec.new()
    file.to_string()
    model.entities.push(entity)
    Vec.new()
    node.child_by_field_name("parameters")
    collect_parameters_from_list(&param_list, source, &mut params) {
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_parameter_declaration(&child, source, params)
    }
    Vec.new()
    node.child_by_field_name("parameters")
    collect_parameters_from_list(&param_list, source, &mut params) {
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_parameter_declaration(&child, source, params)
    }
    param_list.walk()
    param_list.children(&mut cursor)
    child.kind()
    parse_parameter_declaration(&child, source, params) {
        node.child_by_field_name("type")
        parse_type_node(&type_node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.kind()
        names.push(node_text(&child, source))
        node_text(&child, source)
        names.is_empty()
        params.push(Parameter {
            name: String::new(),
            type_info,
        })
        String.new()
        params.push(Parameter {
                name,
                type_info: clone_type_info(&type_info),
            })
        clone_type_info(&type_info)
    }
    node.child_by_field_name("type")
    parse_type_node(&type_node, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    Vec.new()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    names.push(node_text(&child, source))
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    names.is_empty()
    params.push(Parameter {
            name: String::new(),
            type_info,
        })
    String.new()
    params.push(Parameter {
                name,
                type_info: clone_type_info(&type_info),
            })
    clone_type_info(&type_info) {
        TypeInfo.Simple(s.clone())
        s.clone()
        base.clone()
        params.iter().map(clone_type_info).collect()
        params.iter().map(clone_type_info)
        params.iter()
        TypeInfo.Optional(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Collection(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Tuple(items.iter().map(clone_type_info).collect())
        items.iter().map(clone_type_info).collect()
        items.iter().map(clone_type_info)
        items.iter()
        TypeInfo.Reference(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
    }
    node.child_by_field_name("result")
    result_node.kind()
    Vec.new()
    result_node.walk()
    result_node.children(&mut cursor)
    child.kind()
    child.child_by_field_name("type")
    items.push(parse_type_node(&type_n, source))
    parse_type_node(&type_n, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    items.len()
    Some(items.remove(0))
    items.remove(0)
    items.is_empty()
    Some(TypeInfo::Tuple(items))
    TypeInfo.Tuple(items)
    Some(parse_type_node(&result_node, source))
    parse_type_node(&result_node, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    node.kind()
    parse_call_expr(node, source) {
        node.child_by_field_name("function")
        extract_arguments(node, source)
        func_node.kind()
        func_node
                .child_by_field_name("value").map(|n| node_text(&n, source))
        func_node.child_by_field_name("value")
        node_text(&n, source)
        func_node
                .child_by_field_name("field")
                .map(|n| node_text(&n, source)).unwrap_or_default()
        func_node
                .child_by_field_name("field").map(|n| node_text(&n, source))
        func_node.child_by_field_name("field")
        node_text(&n, source)
        Some(CallExpr {
                receiver,
                method,
                arguments,
            })
        node_text(&func_node, source)
        text.rsplit_once("::")
        Some(CallExpr {
                    receiver: Some(recv.to_string()),
                    method: method.to_string(),
                    arguments,
                })
        Some(recv.to_string())
        recv.to_string()
        method.to_string()
        Some(CallExpr {
                    receiver: None,
                    method: text,
                    arguments,
                })
        node_text(&func_node, source)
        Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    }
    calls.push(call)
    node.walk()
    node.children(&mut cursor)
    collect_call_exprs(&child, source, calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    node.child_by_field_name("function")
    extract_arguments(node, source) {
        Vec.new()
        node.child_by_field_name("arguments")
        arg_list.walk()
        arg_list.children(&mut cursor)
        child.kind()
        child.kind()
        child.kind()
        args.push(node_text(&child, source))
        node_text(&child, source)
    }
    func_node.kind()
    func_node
                .child_by_field_name("operand").map(|n| node_text(&n, source))
    func_node.child_by_field_name("operand")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    func_node
                .child_by_field_name("field")
                .map(|n| node_text(&n, source)).unwrap_or_default()
    func_node
                .child_by_field_name("field").map(|n| node_text(&n, source))
    func_node.child_by_field_name("field")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver,
                method,
                arguments,
            })
    node_text(&func_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    node_text(&func_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    Vec.new()
    node.child_by_field_name("arguments")
    arg_list.walk()
    arg_list.children(&mut cursor)
    child.is_named()
    args.push(node_text(&child, source))
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.kind()
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    TypeInfo.Reference(Box::new(parse_type_node(&child, source)))
    Box.new(parse_type_node(&child, source))
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.child_by_field_name("element")
    TypeInfo.Collection(Box::new(parse_type_node(&elem, source)))
    Box.new(parse_type_node(&elem, source))
    parse_type_node(&elem, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.child_by_field_name("element")
    TypeInfo.Collection(Box::new(parse_type_node(&elem, source)))
    Box.new(parse_type_node(&elem, source))
    parse_type_node(&elem, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node
                .child_by_field_name("key").map(|n| parse_type_node(&n, source))
    node.child_by_field_name("key")
    parse_type_node(&n, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    node
                .child_by_field_name("value").map(|n| parse_type_node(&n, source))
    node.child_by_field_name("value")
    parse_type_node(&n, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    "map".to_string()
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple("interface{}".to_string())
    "interface{}".to_string()
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    is_embedded_field(field) {
        inner.as_ref()
    }
    extract_type_name_from_info(&field.type_info) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
        extract_type_name_from_info(inner)
    }
    relationships.push(Relationship::Inheritance {
                    child: owner.to_string(),
                    parent,
                })
    owner.to_string()
    infer_from_type(owner, &field.name, &field.type_info, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    inner.as_ref()
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    Some(name.clone())
    name.clone()
    extract_type_name_from_info(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
        extract_type_name_from_info(inner)
    }
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
    owner.to_string()
    name.clone()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
    owner.to_string()
    field_name.to_string()
    inner.as_ref()
    extract_user_type_name(ptr_inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Aggregation {
                            from: owner.to_string(),
                            to: name,
                            field_name: field_name.to_string(),
                            cardinality: Cardinality::ZeroOrMore,
                        })
    owner.to_string()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Composition {
                            owner: owner.to_string(),
                            owned: name,
                            field_name: field_name.to_string(),
                            cardinality: Cardinality::ZeroOrMore,
                        })
    owner.to_string()
    field_name.to_string()
    name.is_empty()
    name.chars().next().unwrap()
    name.chars().next()
    name.chars()
    first.is_uppercase()
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    Some(name.clone())
    name.clone()
    name.is_empty()
    name.chars().next().unwrap()
    name.chars().next()
    name.chars()
    first.is_uppercase()
    source[node.byte_range()].to_string()
    node.byte_range()
    node.child_by_field_name(field).map(|n| node_text(&n, source))
    node.child_by_field_name(field)
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple(s.clone())
    s.clone()
    base.clone()
    params.iter().map(clone_type_info).collect()
    params.iter().map(clone_type_info)
    params.iter()
    TypeInfo.Optional(Box::new(clone_type_info(inner)))
    Box.new(clone_type_info(inner))
    clone_type_info(inner) {
        TypeInfo.Simple(s.clone())
        s.clone()
        base.clone()
        params.iter().map(clone_type_info).collect()
        params.iter().map(clone_type_info)
        params.iter()
        TypeInfo.Optional(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Collection(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Tuple(items.iter().map(clone_type_info).collect())
        items.iter().map(clone_type_info).collect()
        items.iter().map(clone_type_info)
        items.iter()
        TypeInfo.Reference(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
    }
    TypeInfo.Collection(Box::new(clone_type_info(inner)))
    Box.new(clone_type_info(inner))
    clone_type_info(inner) {
        TypeInfo.Simple(s.clone())
        s.clone()
        base.clone()
        params.iter().map(clone_type_info).collect()
        params.iter().map(clone_type_info)
        params.iter()
        TypeInfo.Optional(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Collection(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Tuple(items.iter().map(clone_type_info).collect())
        items.iter().map(clone_type_info).collect()
        items.iter().map(clone_type_info)
        items.iter()
        TypeInfo.Reference(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
    }
    TypeInfo.Tuple(items.iter().map(clone_type_info).collect())
    items.iter().map(clone_type_info).collect()
    items.iter().map(clone_type_info)
    items.iter()
    TypeInfo.Reference(Box::new(clone_type_info(inner)))
    Box.new(clone_type_info(inner))
    clone_type_info(inner) {
        TypeInfo.Simple(s.clone())
        s.clone()
        base.clone()
        params.iter().map(clone_type_info).collect()
        params.iter().map(clone_type_info)
        params.iter()
        TypeInfo.Optional(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Collection(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
        TypeInfo.Tuple(items.iter().map(clone_type_info).collect())
        items.iter().map(clone_type_info).collect()
        items.iter().map(clone_type_info)
        items.iter()
        TypeInfo.Reference(Box::new(clone_type_info(inner)))
        Box.new(clone_type_info(inner))
        clone_type_info(inner)
    }
    root.walk()
    root.children(&mut cursor)
    child.kind()
    handle_class(&child, source, file, model) {
        parse_class(node, source, file)
        extract_class_heritage(node, source, &entity.name, &mut model.relationships)
        infer_field_relationships(&entity, &mut model.relationships)
        model.entities.push(entity)
    }
    handle_interface(&child, source, file, model) {
        parse_interface(node, source, file)
        extract_interface_heritage(node, source, &entity.name, &mut model.relationships)
        infer_field_relationships(&entity, &mut model.relationships)
        model.entities.push(entity)
    }
    parse_enum(&child, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        node.child_by_field_name("body")
        variant_list.walk()
        variant_list.children(&mut cursor)
        child.kind()
        child_by_field(&child, "name", source)
        fields.push(Field {
                        name: variant_name,
                        type_info: TypeInfo::Simple("()".to_string()),
                        visibility: Visibility::Public,
                    })
        TypeInfo.Simple("()".to_string())
        "()".to_string()
        Some(Entity {
        name,
        kind: EntityKind::Enum,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    parse_type_alias(&child, source, file) {
        child_by_field(node, "name", source)
        Some(Entity {
        name,
        kind: EntityKind::TypeAlias,
        fields: Vec::new(),
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    parse_function(&child, source, file) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        Vec.new()
        node.child_by_field_name("body")
        collect_call_exprs(&body, source, &mut calls)
        Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
        file.to_string()
    }
    model.functions.push(func)
    child.walk()
    child.children(&mut inner_cursor)
    inner_child.kind()
    handle_class(&inner_child, source, file, model) {
        parse_class(node, source, file)
        extract_class_heritage(node, source, &entity.name, &mut model.relationships)
        infer_field_relationships(&entity, &mut model.relationships)
        model.entities.push(entity)
    }
    handle_interface(&inner_child, source, file, model) {
        parse_interface(node, source, file)
        extract_interface_heritage(node, source, &entity.name, &mut model.relationships)
        infer_field_relationships(&entity, &mut model.relationships)
        model.entities.push(entity)
    }
    parse_enum(&inner_child, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        node.child_by_field_name("body")
        variant_list.walk()
        variant_list.children(&mut cursor)
        child.kind()
        child_by_field(&child, "name", source)
        fields.push(Field {
                        name: variant_name,
                        type_info: TypeInfo::Simple("()".to_string()),
                        visibility: Visibility::Public,
                    })
        TypeInfo.Simple("()".to_string())
        "()".to_string()
        Some(Entity {
        name,
        kind: EntityKind::Enum,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    parse_type_alias(&inner_child, source, file) {
        child_by_field(node, "name", source)
        Some(Entity {
        name,
        kind: EntityKind::TypeAlias,
        fields: Vec::new(),
        methods: Vec::new(),
        source_file: file.to_string(),
    })
        Vec.new()
        Vec.new()
        file.to_string()
    }
    model.entities.push(entity)
    parse_function(&inner_child, source, file) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        Vec.new()
        node.child_by_field_name("body")
        collect_call_exprs(&body, source, &mut calls)
        Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
        file.to_string()
    }
    model.functions.push(func)
    parse_class(node, source, file) {
        child_by_field(node, "name", source)
        Vec.new()
        Vec.new()
        node.child_by_field_name("body")
        body.walk()
        body.children(&mut cursor)
        child.kind()
        parse_class_field(&child, source)
        fields.push(field)
        parse_class_method(&child, source)
        methods.push(method)
        Some(Entity {
        name,
        kind: EntityKind::Class,
        fields,
        methods,
        source_file: file.to_string(),
    })
        file.to_string()
    }
    extract_class_heritage(node, source, &entity.name, &mut model.relationships) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        child.walk()
        child.children(&mut heritage_cursor)
        clause.kind()
        clause.walk()
        clause.children(&mut clause_cursor)
        type_child.is_named()
        node_text(&type_child, source)
        relationships.push(Relationship::Inheritance {
                                    child: class_name.to_string(),
                                    parent: parent_name,
                                })
        class_name.to_string()
        clause.walk()
        clause.children(&mut clause_cursor)
        type_child.is_named()
        node_text(&type_child, source)
        relationships.push(Relationship::Implementation {
                                    implementor: class_name.to_string(),
                                    interface: iface_name,
                                })
        class_name.to_string()
    }
    infer_field_relationships(&entity, &mut model.relationships) {
        infer_from_type(owner, &field.name, &field.type_info, relationships)
    }
    model.entities.push(entity)
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Vec.new()
    Vec.new()
    node.child_by_field_name("body")
    body.walk()
    body.children(&mut cursor)
    child.kind()
    parse_class_field(&child, source) {
        child_by_field(node, "name", source)
        extract_accessibility(node, source)
        has_optional_marker(node)
        node.child_by_field_name("type")
        parse_type_node(&type_ann, source)
        wrap_optional_if_needed(ti)
        TypeInfo.Simple("unknown".to_string())
        "unknown".to_string()
        Some(Field {
        name,
        type_info,
        visibility,
    })
    }
    fields.push(field)
    parse_class_method(&child, source) {
        child_by_field(node, "name", source)
        extract_accessibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        has_child_kind(node, "static")
        has_child_kind(node, "abstract")
        Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract,
    })
    }
    methods.push(method)
    Some(Entity {
        name,
        kind: EntityKind::Class,
        fields,
        methods,
        source_file: file.to_string(),
    })
    file.to_string()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    child.walk()
    child.children(&mut heritage_cursor)
    clause.kind()
    clause.walk()
    clause.children(&mut clause_cursor)
    type_child.is_named()
    node_text(&type_child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    relationships.push(Relationship::Inheritance {
                                    child: class_name.to_string(),
                                    parent: parent_name,
                                })
    class_name.to_string()
    clause.walk()
    clause.children(&mut clause_cursor)
    type_child.is_named()
    node_text(&type_child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    relationships.push(Relationship::Implementation {
                                    implementor: class_name.to_string(),
                                    interface: iface_name,
                                })
    class_name.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_accessibility(node, source) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        node_text(&child, source)
        text.as_str()
    }
    has_optional_marker(node) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
    }
    node.child_by_field_name("type")
    parse_type_node(&type_ann, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    wrap_optional_if_needed(ti) {
        TypeInfo.Optional(Box::new(other))
        Box.new(other)
    }
    TypeInfo.Simple("unknown".to_string())
    "unknown".to_string()
    Some(Field {
        name,
        type_info,
        visibility,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    extract_accessibility(node, source) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        node_text(&child, source)
        text.as_str()
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_return_type(node, source) {
        node.child_by_field_name("return_type")
        Some(parse_type_node(&ret, source))
        parse_type_node(&ret, source)
    }
    has_child_kind(node, "static") {
        node.walk()
        node.children(&mut cursor)
        child.kind()
    }
    has_child_kind(node, "abstract") {
        node.walk()
        node.children(&mut cursor)
        child.kind()
    }
    Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract,
    })
    parse_interface(node, source, file) {
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.kind()
        child.kind()
        parse_method_elem(&child, source)
        methods.push(method)
        Some(Entity {
        name: name.to_string(),
        kind: EntityKind::Interface,
        fields: Vec::new(),
        methods,
        source_file: file.to_string(),
    })
        name.to_string()
        Vec.new()
        file.to_string()
    }
    extract_interface_heritage(node, source, &entity.name, &mut model.relationships) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
        child.walk()
        child.children(&mut clause_cursor)
        type_child.is_named()
        node_text(&type_child, source)
        relationships.push(Relationship::Inheritance {
                        child: iface_name.to_string(),
                        parent: parent_name,
                    })
        iface_name.to_string()
    }
    infer_field_relationships(&entity, &mut model.relationships) {
        infer_from_type(owner, &field.name, &field.type_info, relationships)
    }
    model.entities.push(entity)
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Vec.new()
    Vec.new()
    node.child_by_field_name("body")
    body.walk()
    body.children(&mut cursor)
    child.kind()
    parse_property_signature(&child, source) {
        child_by_field(node, "name", source)
        has_optional_marker(node)
        node.child_by_field_name("type")
        parse_type_node(&type_ann, source)
        wrap_optional_if_needed(ti)
        TypeInfo.Simple("unknown".to_string())
        "unknown".to_string()
        Some(Field {
        name,
        type_info,
        visibility: Visibility::Public,
    })
    }
    fields.push(field)
    parse_method_signature(&child, source) {
        child_by_field(node, "name", source)
        extract_visibility(node, source)
        parse_parameters(node, source)
        parse_return_type(node, source)
        has_self_parameter(node, source)
        Some(Method {
        name,
        parameters,
        return_type,
        visibility,
        is_static,
        is_abstract,
    })
    }
    methods.push(method)
    Some(Entity {
        name,
        kind: EntityKind::Interface,
        fields,
        methods,
        source_file: file.to_string(),
    })
    file.to_string()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    child.walk()
    child.children(&mut clause_cursor)
    type_child.is_named()
    node_text(&type_child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    relationships.push(Relationship::Inheritance {
                        child: iface_name.to_string(),
                        parent: parent_name,
                    })
    iface_name.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    has_optional_marker(node) {
        node.walk()
        node.children(&mut cursor)
        child.kind()
    }
    node.child_by_field_name("type")
    parse_type_node(&type_ann, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    wrap_optional_if_needed(ti) {
        TypeInfo.Optional(Box::new(other))
        Box.new(other)
    }
    TypeInfo.Simple("unknown".to_string())
    "unknown".to_string()
    Some(Field {
        name,
        type_info,
        visibility: Visibility::Public,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_return_type(node, source) {
        node.child_by_field_name("return_type")
        Some(parse_type_node(&ret, source))
        parse_type_node(&ret, source)
    }
    Some(Method {
        name,
        parameters,
        return_type,
        visibility: Visibility::Public,
        is_static: false,
        is_abstract: true,
    })
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    body.walk()
    body.children(&mut cursor)
    child.kind()
    fields.push(Field {
                    name: node_text(&child, source),
                    type_info: TypeInfo::Simple("()".to_string()),
                    visibility: Visibility::Public,
                })
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple("()".to_string())
    "()".to_string()
    child.kind()
    child_by_field(&child, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    fields.push(Field {
                        name: member_name,
                        type_info: TypeInfo::Simple("()".to_string()),
                        visibility: Visibility::Public,
                    })
    TypeInfo.Simple("()".to_string())
    "()".to_string()
    Some(Entity {
        name,
        kind: EntityKind::Enum,
        fields,
        methods: Vec::new(),
        source_file: file.to_string(),
    })
    Vec.new()
    file.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    Some(Entity {
        name,
        kind: EntityKind::TypeAlias,
        fields: Vec::new(),
        methods: Vec::new(),
        source_file: file.to_string(),
    })
    Vec.new()
    Vec.new()
    file.to_string()
    child_by_field(node, "name", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    parse_parameters(node, source) {
        Vec.new()
        node.child_by_field_name("parameters")
        param_list.walk()
        param_list.children(&mut cursor)
        child.kind()
        parse_single_parameter(&child, source)
        params.push(param)
    }
    parse_return_type(node, source) {
        node.child_by_field_name("return_type")
        Some(parse_type_node(&ret, source))
        parse_type_node(&ret, source)
    }
    Vec.new()
    node.child_by_field_name("body")
    collect_call_exprs(&body, source, &mut calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    Some(Function {
        name,
        parameters,
        return_type,
        visibility,
        calls,
        source_file: file.to_string(),
    })
    file.to_string()
    Vec.new()
    node.child_by_field_name("parameters")
    param_list.walk()
    param_list.children(&mut cursor)
    child.kind()
    parse_single_parameter(&child, source) {
        child_by_field(node, "pattern", source)
        node.child_by_field_name("type")
        parse_type_node(&type_node, source)
        Some(Parameter { name, type_info })
    }
    params.push(param)
    child_by_field(node, "pattern", source) {
        node.child_by_field_name(field).map(|n| node_text(&n, source))
        node.child_by_field_name(field)
        node_text(&n, source)
    }
    node.child_by_field_name("type")
    parse_type_node(&type_ann, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple("unknown".to_string())
    "unknown".to_string()
    Some(Parameter { name, type_info })
    node.child_by_field_name("return_type")
    ret.walk()
    ret.children(&mut cursor)
    child.is_named()
    Some(parse_type_node(&child, source))
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    Some(parse_type_node(&ret, source))
    parse_type_node(&ret, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    node.kind()
    parse_call_expr(node, source) {
        node.child_by_field_name("function")
        extract_arguments(node, source)
        func_node.kind()
        func_node
                .child_by_field_name("value").map(|n| node_text(&n, source))
        func_node.child_by_field_name("value")
        node_text(&n, source)
        func_node
                .child_by_field_name("field")
                .map(|n| node_text(&n, source)).unwrap_or_default()
        func_node
                .child_by_field_name("field").map(|n| node_text(&n, source))
        func_node.child_by_field_name("field")
        node_text(&n, source)
        Some(CallExpr {
                receiver,
                method,
                arguments,
            })
        node_text(&func_node, source)
        text.rsplit_once("::")
        Some(CallExpr {
                    receiver: Some(recv.to_string()),
                    method: method.to_string(),
                    arguments,
                })
        Some(recv.to_string())
        recv.to_string()
        method.to_string()
        Some(CallExpr {
                    receiver: None,
                    method: text,
                    arguments,
                })
        node_text(&func_node, source)
        Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    }
    calls.push(call)
    node.walk()
    node.children(&mut cursor)
    collect_call_exprs(&child, source, calls) {
        node.kind()
        parse_call_expr(node, source)
        calls.push(call)
        node.walk()
        node.children(&mut cursor)
        collect_call_exprs(&child, source, calls)
    }
    node.child_by_field_name("function")
    extract_arguments(node, source) {
        Vec.new()
        node.child_by_field_name("arguments")
        arg_list.walk()
        arg_list.children(&mut cursor)
        child.kind()
        child.kind()
        child.kind()
        args.push(node_text(&child, source))
        node_text(&child, source)
    }
    func_node.kind()
    func_node
                .child_by_field_name("object").map(|n| node_text(&n, source))
    func_node.child_by_field_name("object")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    func_node
                .child_by_field_name("property")
                .map(|n| node_text(&n, source)).unwrap_or_default()
    func_node
                .child_by_field_name("property").map(|n| node_text(&n, source))
    func_node.child_by_field_name("property")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver,
                method,
                arguments,
            })
    node_text(&func_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    node_text(&func_node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Some(CallExpr {
                receiver: None,
                method: text,
                arguments,
            })
    Vec.new()
    node.child_by_field_name("arguments")
    arg_list.walk()
    arg_list.children(&mut cursor)
    child.is_named()
    args.push(node_text(&child, source))
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.kind()
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    parse_generic_type(node, source) {
        node
        .child_by_field_name("type")
        .map(|n| node_text(&n, source)).unwrap_or_default()
        node
        .child_by_field_name("type").map(|n| node_text(&n, source))
        node.child_by_field_name("type")
        node_text(&n, source)
        Vec.new()
        node.child_by_field_name("type_arguments")
        type_args.walk()
        type_args.children(&mut cursor)
        child.is_named()
        params.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        base.as_str()
        params.len()
        TypeInfo.Optional(Box::new(params.remove(0)))
        Box.new(params.remove(0))
        params.remove(0)
        params.len()
        TypeInfo.Collection(Box::new(params.remove(0)))
        Box.new(params.remove(0))
        params.remove(0)
    }
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    TypeInfo.Collection(Box::new(parse_type_node(&child, source)))
    Box.new(parse_type_node(&child, source))
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    parse_union_type(node, source) {
        node.walk()
        Vec.new()
        node.children(&mut cursor)
        child.is_named()
        members.push(child)
        members
        .iter()
        .filter(|m| {
            let text = node_text(m, source);
            !null_types.contains(&text.as_str())
        }).collect()
        members
        .iter().filter(|m| {
            let text = node_text(m, source);
            !null_types.contains(&text.as_str())
        })
        members.iter()
        node_text(m, source)
        null_types.contains(&text.as_str())
        text.as_str()
        members.iter().any(|m| {
        let text = node_text(m, source);
        null_types.contains(&text.as_str())
    })
        members.iter()
        node_text(m, source)
        null_types.contains(&text.as_str())
        text.as_str()
        non_null.len()
        TypeInfo.Optional(Box::new(parse_type_node(non_null[0], source)))
        Box.new(parse_type_node(non_null[0], source))
        parse_type_node(non_null[0], source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node.walk()
    node.children(&mut cursor)
    child.is_named()
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    node
        .child_by_field_name("name")
        .map(|n| node_text(&n, source)).unwrap_or_default()
    node
        .child_by_field_name("name").map(|n| node_text(&n, source))
    node.child_by_field_name("name")
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    Vec.new()
    node.child_by_field_name("type_arguments")
    type_args.walk()
    type_args.children(&mut cursor)
    child.is_named()
    params.push(parse_type_node(&child, source))
    parse_type_node(&child, source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    params.len()
    TypeInfo.Collection(Box::new(params.remove(0)))
    Box.new(params.remove(0))
    params.remove(0)
    node.walk()
    Vec.new()
    node.children(&mut cursor)
    child.is_named()
    members.push(child)
    members
        .iter()
        .filter(|m| {
            let text = node_text(m, source);
            !null_types.contains(&text.as_str())
        }).collect()
    members
        .iter().filter(|m| {
            let text = node_text(m, source);
            !null_types.contains(&text.as_str())
        })
    members.iter()
    node_text(m, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    null_types.contains(&text.as_str())
    text.as_str()
    members.iter().any(|m| {
        let text = node_text(m, source);
        null_types.contains(&text.as_str())
    })
    members.iter()
    node_text(m, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    null_types.contains(&text.as_str())
    text.as_str()
    non_null.len()
    TypeInfo.Optional(Box::new(parse_type_node(non_null[0], source)))
    Box.new(parse_type_node(non_null[0], source))
    parse_type_node(non_null[0], source) {
        node.kind()
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        parse_generic_type(node, source)
        node.child_by_field_name("type")
        TypeInfo.Reference(Box::new(parse_type_node(&inner, source)))
        Box.new(parse_type_node(&inner, source))
        parse_type_node(&inner, source)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
        Vec.new()
        node.walk()
        node.children(&mut cursor)
        child.is_named()
        items.push(parse_type_node(&child, source))
        parse_type_node(&child, source)
        TypeInfo.Tuple(items)
        TypeInfo.Simple(node_text(node, source))
        node_text(node, source)
    }
    TypeInfo.Simple(node_text(node, source))
    node_text(node, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    infer_from_type(owner, &field.name, &field.type_info, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
    owner.to_string()
    name.clone()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
    owner.to_string()
    field_name.to_string()
    extract_user_type_name(inner) {
        is_user_type(name)
        Some(name.clone())
        name.clone()
    }
    relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrMore,
                })
    owner.to_string()
    field_name.to_string()
    infer_from_type(owner, field_name, param, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    infer_from_type(owner, field_name, item, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    infer_from_type(owner, field_name, inner, relationships) {
        is_user_type(name)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name.clone(),
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        name.clone()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::ZeroOrOne,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                    owner: owner.to_string(),
                    owned: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::OneOrMore,
                })
        owner.to_string()
        field_name.to_string()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                    from: owner.to_string(),
                    to: name,
                    field_name: field_name.to_string(),
                    cardinality: Cardinality::One,
                })
        owner.to_string()
        field_name.to_string()
        base.as_str()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Aggregation {
                                from: owner.to_string(),
                                to: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        params.first()
        extract_user_type_name(inner)
        relationships.push(Relationship::Composition {
                                owner: owner.to_string(),
                                owned: name,
                                field_name: field_name.to_string(),
                                cardinality: Cardinality::One,
                            })
        owner.to_string()
        field_name.to_string()
        infer_from_type(owner, field_name, param, relationships)
        infer_from_type(owner, field_name, item, relationships)
    }
    name.is_empty()
    name.chars().next().unwrap()
    name.chars().next()
    name.chars()
    first.is_uppercase()
    is_user_type(name) {
        name.is_empty()
        name.chars().next().unwrap()
        name.chars().next()
        name.chars()
        first.is_uppercase()
    }
    Some(name.clone())
    name.clone()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    node_text(&child, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    text.as_str()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    node.walk()
    node.children(&mut cursor)
    child.kind()
    source[node.byte_range()].to_string()
    node.byte_range()
    node.child_by_field_name(field).map(|n| node_text(&n, source))
    node.child_by_field_name(field)
    node_text(&n, source) {
        source[node.byte_range()].to_string()
        node.byte_range()
    }
    TypeInfo.Optional(Box::new(other))
    Box.new(other)
    Box.new(class_diagram::ClassDiagramEmitter)
    Box.new(er_diagram::ErDiagramEmitter)
    Box.new(zenuml::ZenumlEmitter)
    Cli.parse()
    Some(code_documenter::parse::Language::Rust)
    Some(code_documenter::parse::Language::Go)
    Some(code_documenter::parse::Language::TypeScript)
    code_documenter.run(&cli.path, diagram_type, language, cli.entry.as_deref()) {
        CodeModel.new()
        path.is_file()
        parse_single_file(path, language)
        merged.merge(model)
        path.is_dir()
        WalkDir::new(path)
            .into_iter()
            .filter_map(|e| e.ok()).filter(|e| e.file_type().is_file())
        WalkDir::new(path)
            .into_iter().filter_map(|e| e.ok())
        WalkDir::new(path).into_iter()
        WalkDir.new(path)
        e.ok()
        e.file_type().is_file()
        e.file_type()
        entry.path()
        Some(l)
        parse.detect_language(file_path)
        parse_single_file(file_path, Some(lang))
        Some(lang)
        merged.merge(model)
        emit.get_emitter(diagram_type)
        Ok(emitter.emit(&merged))
        emitter.emit(&merged)
    }
    cli.entry.as_deref()
    std::fs.write(&output_path, &result)
    Ok(())
    CodeModel.new()
    path.is_file()
    parse_single_file(path, language) {
        language
        .or_else(|| parse::detect_language(path)).context(format!("Could not detect language for {}", path.display()))
        language.or_else(|| parse::detect_language(path))
        parse.detect_language(path)
        std::fs::read_to_string(path).with_context(|| format!("Failed to read {}", path.display()))
        std::fs.read_to_string(path)
        parse.get_parser(lang)
        parser
        .parse_file(path, &source).with_context(|| format!("Failed to parse {}", path.display()))
        parser.parse_file(path, &source)
    }
    merged.merge(model)
    path.is_dir()
    WalkDir::new(path)
            .into_iter()
            .filter_map(|e| e.ok()).filter(|e| e.file_type().is_file())
    WalkDir::new(path)
            .into_iter().filter_map(|e| e.ok())
    WalkDir::new(path).into_iter()
    WalkDir.new(path)
    e.ok()
    e.file_type().is_file()
    e.file_type()
    entry.path()
    Some(l)
    parse.detect_language(file_path) {
        path.extension()?.to_str()
        path.extension()
        Some(Language::Rust)
        Some(Language::Go)
        Some(Language::TypeScript)
    }
    parse_single_file(file_path, Some(lang)) {
        language
        .or_else(|| parse::detect_language(path)).context(format!("Could not detect language for {}", path.display()))
        language.or_else(|| parse::detect_language(path))
        parse.detect_language(path)
        std::fs::read_to_string(path).with_context(|| format!("Failed to read {}", path.display()))
        std::fs.read_to_string(path)
        parse.get_parser(lang)
        parser
        .parse_file(path, &source).with_context(|| format!("Failed to parse {}", path.display()))
        parser.parse_file(path, &source)
    }
    Some(lang)
    merged.merge(model)
    emit.get_emitter(diagram_type) {
        Box.new(class_diagram::ClassDiagramEmitter)
        Box.new(er_diagram::ErDiagramEmitter)
        Box.new(zenuml::ZenumlEmitter)
    }
    Ok(emitter.emit(&merged))
    emitter.emit(&merged)
    language
        .or_else(|| parse::detect_language(path)).context(format!("Could not detect language for {}", path.display()))
    language.or_else(|| parse::detect_language(path))
    parse.detect_language(path) {
        path.extension()?.to_str()
        path.extension()
        Some(Language::Rust)
        Some(Language::Go)
        Some(Language::TypeScript)
    }
    std::fs::read_to_string(path).with_context(|| format!("Failed to read {}", path.display()))
    std::fs.read_to_string(path)
    parse.get_parser(lang) {
        Box.new(rust::RustParser)
        Box.new(go::GoParser)
        Box.new(typescript::TypeScriptParser)
    }
    parser
        .parse_file(path, &source).with_context(|| format!("Failed to parse {}", path.display()))
    parser.parse_file(path, &source)
